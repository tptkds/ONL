const Form: React.FC = () => {
    return (
        <form className="flex flex-col">
            <input type="email" />
            <input type="password" />
        </form>
    );
};

export default Form;
